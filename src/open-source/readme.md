# open-source

This folder is used to contain open-source projects that, for whatever reason, did not compile properly to electron. Note that prettify may have changed formatting, and for .tsx files `import React from 'react'` was added. 

- rjsf/mui/src - The src folder from https://github.com/rjsf-team/react-jsonschema-form/tree/main/packages/mui